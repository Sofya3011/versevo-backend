{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uvicorn services.main:app --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/api/flutter/health",
    "restartPolicyType": "ON_FAILURE"
  }
}
